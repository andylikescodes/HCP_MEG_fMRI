BootStrap: docker
From: ubuntu:20.04

%post
	apt-get update
	apt-get -y install libqt5core5a
	apt-get -y install curl
	apt -y install connectome-workbench

%startscript
	sudo strip --remove-section=.note.ABI-tag /usr/lib/x86_64-linux-gnu/libQt5Core.so.5
